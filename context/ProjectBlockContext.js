import { createContext } from "react";

export const ProjectBlockContext = createContext();

// const ProjectBlockProvider = ({ children, project }) => {
//   return <ProjectBlockContext.Provider project={project}>{children}</ProjectBlockContext.Provider>;
// };

// export { ProjectBlockProvider };
